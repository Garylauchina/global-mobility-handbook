# 更新与过期政策

移民和国籍规则会快速变化。页面的“开放”只代表核验时点，不是持续保证。

## 复核周期

- 工资联动或最低收入/生活财力门槛、学习许可资金证明、年度配额、限时规则、房地产资格，以及有主管机关材料表明正在快速变化的规则：至少每30天复核。
- 其他当前开放项目：至少每90天复核。
- 已关闭、暂停、被替代和待细则项目：至少每180天复核一次，或在主管机关发布新公告时立即复核。
- C级证据项目：始终标为“候选/待主管机关确认”，获得当前主管机关直接证据前不得升级为开放。

每页 frontmatter 用 `review_interval_days: 30|90|180` 明确周期。同页有多个项目时采用最短适用周期；脚本不得根据措辞自动猜测该字段。

## 过期处理

超过对应周期且未完成复核的 `current` 页面使用 `status: "stale"`，保留上次 `last_verified` 和证据等级，增加标准“复核警示”，并把每个项目块的“当前状态”改为“待复核（最后核验：YYYY-MM-DD；原记录：开放）”，不得继续仅显示“开放”。`stale` 页的 A/B 级仅代表截至 `last_verified` 的证据质量。完成全项复核后才能恢复 `current`；确认关闭、暂停或替代后移入警示目录。候选和警示页超过180天时保持原状态并进入复核队列。规则冲突时保留冲突，不自行选择对申请人更有利的数字。

可运行 `node scripts/audit-freshness.mjs` 生成复核队列。该脚本只根据日期、状态和页面显式填写的 `review_interval_days` 排队，不判断政策是否变化，也不修改页面。

发布流程运行 `node scripts/audit-freshness.mjs --check-public-status`：如果页面已到期却仍标为 `current`，构建应停止；改为标准 `stale` 警示或完成重新核验后才可继续发布。

只有在当日核对页面核心字段后，才能同时修改 frontmatter 的 `last_verified` 和正文核验日期。措辞、排版、导航或链接标签调整不得刷新日期。

## 更新记录

每次规则变化应记录：旧规则、新规则、生效日、来源发布机关、来源URL、访问日期、证据等级变化和修改日期。重要关闭、重开或门槛变化同时写入 `CHANGELOG.md`。

政策内容页的标准项目字段是“当前快照”。自2026-09-02起，重要规则变化还应追加到该页“政策时间线”：分别记录官方公布日、生效日和过渡安排；已公布但未生效的规则不得提前替换当前快照；原仓库错误须标为“资料更正，非政策变化”。无实质变化的例行复核不追加事件，既有事件不得静默改写。

自动化只能检查结构、日期、路径、链接格式和敏感文件，不能自动判断或改写移民规则。
