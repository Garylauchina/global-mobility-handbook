# 更新与过期政策

本文件统一定义全站的月度复核、状态和日期口径；具体操作见[维护手册](./MAINTENANCE.md)。页面的“开放”来自已完成的证据核对，不是持续保证。

## 每月复核与中央记录

全库每月复核一次，覆盖全部政策内容页，包括当前、待复核、候选及警示归档页。分类和国家索引只承担导航。常规维护按月集中进行；用户另行提出的纠错按指定范围处理。

复核日期统一保存在 `.maintenance/review-state.json`：

- `cadence: "monthly"`：全库采用同一月度节奏。
- `last_full_review`：最近一次已完成的全库内容复核基准。
- `page_reviews`：以政策页相对路径为键，记录该页实际完成核心内容复核的日期，格式为 `YYYY-MM-DD`。同页多个项目块必须全部复核后才能推进这条记录；增删或移动政策页时同步维护路径。

每页自实际复核日加一个日历月到期；下月无同日时取月末。例如 9 月 2 日复核后于 10 月 2 日到期，1 月 31 日复核后于 2 月末到期。月更应核对完整政策页清单，不仅处理已经到期的页面。

只有完成当页核心字段与证据的实质复核，才能推进对应中央记录。局部字段修正、编辑排版、导航调整、替换链接标签、发布成功、自动扫描或检索无结果，都不能作为刷新日期的依据。未完成的复核保留原日期并报告证据缺口。

## 首页展示

仅项目首页统一说明每月更新节奏及最近一次全库内容复核基准；中英文首页保持一致。政策页、分类页和国家索引不展示本站核验日期，政策页 frontmatter 也不保存核验日期或复核周期。

只有本轮已实质复核完整政策页清单，才能推进 `last_full_review` 并同步首页基准。抽样、部分页面更新或单纯发布不能宣称全库完成复核。保留中央记录和 Git 历史作为逐页审计依据，不以隐藏日期冒充更新。

统一基准不得晚于任何政策页的中央复核日期。全库复核跨多日时，取本轮逐页记录中的最早日期作为保守基准，不能用完成日或部署日覆盖较早复核页面的实际时间。

官方公布日、政策生效日、截止日和过渡期是政策事实，继续在相关页面保留，与本站复核日期分开。

## 状态与过期处理

- `current`：当前法律或操作入口可辨认，核心条件有 A/B 级证据支持。
- `stale`：原为 `current`，现已到期且尚未完成本轮复核。保留原证据等级和中央记录；A/B 只表示上次已完成复核的证据质量，不保证当前规则仍有效。
- `candidate-unverified`：证据 C，明确标为“候选/待主管机关确认”；当前主管机关依据不足时不得展示为开放。
- `archived-or-unverified`：关闭、暂停、被替代或无法可靠确认的项目，仅放在 `closed-paused-unverified/`。

到期的 `current` 页改为 `stale`，在通用免责声明后加入：

> **复核警示：** 本页尚未完成本轮月度复核；在完成主管机关复核前，不应视为当前开放规则。

同时将每个项目块的“当前状态”改为“待复核（原记录：开放）”。完成全项复核后才恢复 `current`。候选和警示页到期时保持原状态并进入同一月度复核清单。

404、网站不可访问、未检索到资料或缺少英文页面都不能证明关闭；确认关闭、暂停、替代或重开需要积极证据。官方来源冲突须明确保留，不自行选择对申请人更有利的规则。

## 发布检查与更新记录

时效审计只依据中央记录生成队列，不判断政策变化、不修改内容。发布门禁 `--check-public-status` 拒绝到期却仍标为 `current` 的页面；已有标准警示的 `stale`、候选和归档页可以发布，但不能因此宣布全库复核完成。审计的 `--strict` 在任意政策页到期时失败，适合复核验收，不替代发布门禁。

重要关闭、重开、门槛和分类变化写入 `CHANGELOG.md`。单纯转为 `stale` 只在提交或 PR 中记录。提交说明应区分旧规则、新规则、官方公布与生效日期、主管机关来源、证据变化和实际复核范围；访问及本站复核日期保存在中央记录或 Git 说明中。

政策页标准字段展示当前快照，重要变化追加到“政策时间线”。已公布未生效的规则不得提前替换当前规则；资料错误须标为“资料更正，非政策变化”。时间线保留官方日期、过渡安排、来源及事件证据等级，不展示本站核验日期。无实质变化的复核不追加事件，既有事件不得静默覆盖。
