# 维护手册

本手册用于让维护者、未来迁移后的 Codex 或其他自动化工具在不依赖私人记忆的情况下继续维护仓库和网站。事实边界仍以[研究方法](./METHODOLOGY.md)、[更新政策](./UPDATE_POLICY.md)、[独立性声明](./INDEPENDENCE.md)和[免责声明](./DISCLAIMER.md)为准。

## 维护分层

- `AGENTS.md`：Codex 进入仓库时读取的项目级底线。
- `.agents/skills/global-mobility-maintenance/`：复核、增删、迁移和发布前验证的可移植工作流。
- `scripts/`：只执行确定性检查、日期排队、导航生成和站点构建。
- `.github/workflows/`：在 GitHub 上验证仓库并部署 Pages。

任何一层都不能自动认定移民规则已经变化。网站由 GitHub Pages 托管，Codex 迁移不会中断已经发布的网站。

## 建议节奏

- 主管机关发布关闭、重开、门槛或资格变化时：立即复核受影响页面。
- 工资联动或最低收入/生活财力门槛、年度配额、限时规则、房地产资格，以及有主管机关材料表明正在快速变化的规则：至少每30天。
- 其他当前开放项目：至少每90天。
- 已关闭、暂停、被替代和待主管机关确认的项目：至少每180天。
- 每次准备发布前：运行完整仓库与网站验证。

运行以下命令生成当前复核队列：

```bash
node scripts/audit-freshness.mjs
```

它直接读取每页 frontmatter 的 `review_interval_days`，不会因措辞、语言或项目块顺序而改变周期。可以用固定日期复现结果：

```bash
node scripts/audit-freshness.mjs --as-of 2026-12-01
```

日期到期只说明“需要核验”，不说明项目已经变化。命令不会修改页面。

如果到期的 `current` 页面不能在本轮完成主管机关复核，应保留原 `last_verified` 和证据等级，将 frontmatter 状态改为 `stale`，在页面通用免责声明后加入：

> **复核警示：** 本页已超过复核周期。以下内容最后核验于 YYYY-MM-DD；在完成主管机关复核前，不应视为当前开放规则。

同时把每个项目块的“当前状态”统一改为“待复核（最后核验：YYYY-MM-DD；原记录：开放）”。`stale` 页保留的 A/B 级只代表截至 `last_verified` 的证据质量，不表示当前来源仍可访问或规则仍有效。完成全项复核后才能恢复 `current`；取得关闭、暂停或替代的积极证据后才移入警示目录。候选页和警示页本来就不作为当前开放项目展示，超过180天时保持原状态并进入复核队列。

## AI批量复核与基准测试

AI可以显著降低检索和初稿时间，因此不应继续按人工逐页25—45分钟估算月度工时；但速度不能替代证据门槛。批量复核采用有边界的并行分组，并把墙钟时间与各复核通道的累计工作时间分开记录。

- 明确本轮是抽样基准还是全库复核；抽样结果不得写成“全库已核验”。
- 每页先由一条通道独立逐字段复核，再随机交给不同通道盲审；第二位复核者不得先看第一轮结论。
- 两轮都必须核实完整核心字段、公告日、生效日、过渡期、配额、国籍限制和官方来源冲突。
- 结果使用 `verified-no-change`、`material-change-detected`、`future-change-announced` 或 `evidence-incomplete`。只有争议已经按官方原文裁决、核心字段均能积极重建时，才推进核验日期。
- 官方网页截图不是默认要求。除非用户要求留存，只记录直接URL、文档标题、条款或章节、公布/生效日和访问日；下载的网页或PDF不得提交仓库。

具体抽样、盲审、计时和裁决规则见仓库 Skill 的 `references/benchmark-and-timeline.md`。

## 单页复核

1. 编辑前先运行结构与导航检查，记录既有失败；不要擅自扩大范围修复无关页面。
2. 打开国家页及其当前来源，确认项目官方名称和主管机关。
3. 逐项核对当前状态、门槛、资金性质、首次身份、居住与续签、工作权、家属、长期路径、税务警示和关键限制。
4. 记录公告日、生效日和过渡期；不要把三者混为一谈。
5. 官方页面冲突时保留冲突；不要自行采用对申请人更有利的规则。
6. 只有核心字段均已完成当日核验，才能同时更新 frontmatter 的 `last_verified` 和正文核验日期。
7. 重要关闭、重开、门槛或分类变化写入 `CHANGELOG.md`。

只修改措辞、排版、导航或链接标签时，不更新核验日期。只找到二级来源时，不把项目升级为开放。

如果最后一次主管机关链接暂时失效，先查找同一机关的新页面、法规库、公报、官方存档和当地语言页面。仍无替代来源时，保留最后一次权威链接，在复核警示或变更说明中记录不可访问，不以商业链接替换。单纯由 `current` 转为 `stale` 是复核状态变化，不写入 `CHANGELOG.md`；写入提交或 PR 说明即可。

## 增加、移动或归档页面

- 新方案必须符合收录范围，并先检查警示目录中是否已有旧名称或前身。
- 完成本轮核验且仍开放的项目使用 `status: "current"` 和 A/B 级证据。
- 原本开放但超过复核周期、且尚未完成新一轮核验的页面使用 `status: "stale"`，保留最后一次 A/B 级证据和原核验日期。
- 缺少当前主管机关支持的候选项目使用 `status: "candidate-unverified"`、证据 C 和醒目警示。
- 关闭、暂停、被替代或无法可靠确认的项目放入 `closed-paused-unverified/`；网页失效本身不能证明关闭。
- 页面移动后同时修改原类别和新类别的 `README.md`，然后运行 `node scripts/generate-site-config.mjs`。
- 一个警示页可以包含同一国家的多个历史项目；只有全部项目块都完成复核，才同时推进页面 `last_verified` 和各项目块的核验日期。

每个国家页必须显式设置 `review_interval_days`：工资联动或最低收入/生活财力门槛、年度配额、限时规则、房地产资格和明确快速变化规则用30；其他当前或 `stale` 页面用90；候选和警示页用180。同页存在多个项目时采用最短适用周期。新增或实质改变项目时重新判断该字段，不依靠正文关键词自动推断。

国家页保持既有结构，不建立跨国总表、排名或推荐清单。

## 发布前验证

```bash
node scripts/validate-repo.mjs
node scripts/audit-freshness.mjs --check-public-status
node scripts/generate-site-config.mjs --check
node scripts/run-mkdocs.mjs build --strict --site-dir .site
```

如果修改了仓库 Skill，还要使用当前 Codex 所带 `skill-creator` 的 `quick_validate.py` 检查。应在当前安装中定位该校验器，不把某台电脑的绝对路径写入仓库。

验证通过只说明结构和构建正常，不代表政策结论正确，也不等于获得推送或发布授权。

`--check-public-status` 是发布门禁：它只拒绝“已经到期但仍标为 `current`”的页面；已经明确显示为 `stale`、候选或警示状态的页面仍可发布。`--strict` 则在任何页面到期时返回失败，适合人工复核或未来的提醒任务，不应直接替代发布门禁。

如果编辑前已有无关校验失败，最终说明必须分别列出基线和结束状态，不得声称全部通过，也不得为追求绿色结果越权修复无关页面。`run-mkdocs.mjs` 使用临时配置；相对的 `--site-dir .site` 会由 MkDocs 相对于该临时配置解析，并随临时目录清除。需要检查构建产物或供 CI 上传时，必须传入仓库外的绝对输出路径。缺少依赖时应明确报告未运行，不能用较弱命令替代严格构建。

## 迁移到新环境

1. 克隆 GitHub 仓库并从仓库根目录打开 Codex。
2. Codex 应自动发现 `.agents/skills/global-mobility-maintenance/SKILL.md`；如果没有显示，重启 Codex 后再检查。
3. 安装 Node.js 22、Python 和 `requirements-docs.txt` 中的依赖。
4. 运行上述四项发布前验证。
5. 检查 GitHub 仓库的 Pages 来源仍为 GitHub Actions，并确认最近一次工作流成功。

Skill、维护规则和脚本均使用相对路径，不依赖原电脑的 Codex 记忆、用户名或目录。不要把令牌、私有申请材料或本机绝对路径写入仓库。

首页的“初始全库核验基准日”只有在全库完成复核时才推进；局部更新只改变实际复核过的国家页。普通事实修正把旧规则、新规则、生效日、来源、核验日和证据变化写入提交或 PR 说明；重要关闭、重开、门槛和分类变化另外进入 `CHANGELOG.md`。

## 单项目政策时间线

国家页的标准字段始终展示当前快照；重要变化另外追加到“政策时间线”，不能为了保留历史而让正文继续显示旧规则。

- 自2026-09-02起系统性前向记录；更早事件只在有官方一手材料时选择性补录，不宣称历史完整。
- 已生效变化：更新当前快照并追加事件；已公布未生效变化：保留当前规则、增加未来变化提示并追加事件；仓库原有错误：明确写“资料更正，非政策变化”。
- 每个事件尽量分别记录公布日、生效日、旧规则、新规则、过渡安排、官方来源、本站核验日和证据等级。未知信息标为“未公开”，不得推测。
- 无实质变化的例行复核不写时间线；既有事件不得静默覆盖，后续更正应追加新事件。
